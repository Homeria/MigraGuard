# 🛡️ MigraGuard 프로젝트 기능 명세서 (Functional Specification)

## 1. 프로젝트 정의
**MigraGuard**는 PostgreSQL의 스키마 변경(DDL) 시 발생할 수 있는 서비스 장애 리스크를 사전에 탐지하는 **트래픽 인지형(Traffic-Aware) 데이터베이스 가드레일**입니다. 운영 환경의 실시간 트래픽 패턴과 SQL의 정적 특성을 교차 분석하여 배포 안전성을 정밀하게 검증합니다.

## 2. 핵심 가치 및 목표
- **정밀한 부하 추적**: 누적 지표의 변화량(Delta) 측정을 통한 정확한 시점별 TPS 산출.
- **실환경 모사 검증**: 24시간 트래픽 곡선과 비즈니스 시나리오를 반영한 고충실도 시뮬레이션 환경 제공.
- **보수적 리스크 모델**: 실시간 부하 강도에 따라 유동적으로 변화하는 5단계 위험도 평가 엔진 구축.
- **자동화된 게이트키핑**: 위험 등급(Danger) 감지 시 배포 파이프라인(CI/CD)을 즉시 차단하여 장애 예방.
- **신속한 로직 검증**: 수일간의 데이터 수집 대기 없이 즉각적인 시뮬레이션이 가능한 데이터 인젝션 환경 구축.

## 3. 주요 기능 세부 사항 (v3.8)

### 3.1. 시나리오 기반 데이터 시더 (Data Seeder) - New!
- **즉각적인 데이터 생성**: 실제 시간을 기다리지 않고 1초 만에 7일~30일 치의 가상 시계열 데이터를 SQLite에 주입.
- **다양한 부하 시나리오**:
    - `Normal`: 일정한 주기를 가진 평범한 트래픽 패턴.
    - `Flash Sale`: 특정 시점에 TPS가 급격히(10배 이상) 튀는 돌발 상황 패턴.
    - `Degradation`: 시스템 노후화로 인해 디스크 I/O 성능이 점진적으로 저하되는 패턴.
- **목적**: 적응형 상수 추천 알고리즘(Adaptive Config)의 정확도를 다양한 상황에서 즉시 테스트하기 위함.

### 3.2. 고충실도 부하 생성기 (Load Generator)
- **도메인 기반 부하**: 이커머스 도메인(주문, 재고, 상품)의 복잡한 트랜잭션 상황을 재현.
- **트래픽 곡선 적용**: 사인파(Sine Wave) 공식을 이용해 시간대별 트래픽 변동을 현실적으로 모사.

### 3.3. 지능형 수집 에이전트 (Background Agent)
- **지표 누적 및 정제**: `pg_stat_statements`를 활용하여 테이블별 성능 지표 수집.
- **자동 저장소 관리**: SQLite 저장소의 용량 최적화를 위한 주기적인 가비지 컬렉션(Retention) 수행.

### 3.4. 동적 리스크 평가 엔진 (Risk Engine)
- **설정 유연화 (Extensible Config)**: 임계치, 가중치, 배수 등 엔진의 모든 매직 넘버를 외부(`migraguard.yaml`)에서 조절 가능.
- **5단계 정밀 리포트**: 큐잉 이론 기반의 상세 지표($T_{ddl}, T_{block}, C_{peak}, T_{rec}$)를 정량적으로 제공.
- **배포 골든타임 추천**: 최근 24시간 데이터를 분석하여 가장 안전한 배포 시간대 제시.

## 4. 로드맵 및 마일스톤

### ✅ v3.7: DDL 분석 전면 확대 및 SDK 모듈화 (완료)
- 거의 모든 PostgreSQL DDL 명령어 인식 및 8단계 Lock 모델링 완료.
- 핵심 로직의 패키지(`pkg/migraguard`) 분리 및 CLI 통합 완료.

### 🚀 v3.8: 적응형 설정 및 시나리오 검증 (진행 중)
- **Data Seeder**: 알고리즘 테스트를 위한 시나리오 기반 데이터 주입 기능 개발.
- **Adaptive Config**: 수집된 과거 피크 데이터를 분석하여 리스크 판단 상수(MuMax, DiskIO 등) 자동 추론.

---
*최종 수정일: 2026-04-25 (v3.8 데이터 시더 및 SDK 구조 반영)*
